{
  "name": "bern-mkt-web",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "npx wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:migrate": "node scripts/atualizar-banco.mjs",
    "db:migrate:local": "node scripts/atualizar-banco.mjs --local",
    "db:update": "node scripts/atualizar-banco.mjs",
    "db:update:local": "node scripts/atualizar-banco.mjs --local"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "wrangler": "4.133.0"
  }
}
